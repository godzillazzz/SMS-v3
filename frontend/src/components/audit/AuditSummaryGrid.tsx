type Props = { total: number; pageCount: number; categories: number; actors: number; loading: boolean };
function Card({ label, value, context, tone, loading }: { label: string; value: number | string; context: string; tone: string; loading: boolean }) { return <article className={`audit-metric audit-metric--${tone}`}>{loading ? <div className="audit-metric-skeleton" aria-label="กำลังโหลด" /> : <><span className="audit-metric-icon" aria-hidden="true">◌</span><div><p>{label}</p><strong>{value}</strong><small>{context}</small></div></>}</article>; }
export function AuditSummaryGrid({ total, pageCount, categories, actors, loading }: Props) { return <div className="audit-summary-grid"><Card label="เหตุการณ์ทั้งหมด" value={total} context="จากข้อมูลที่แบ่งหน้า" tone="indigo" loading={loading} /><Card label="เหตุการณ์ในหน้าปัจจุบัน" value={pageCount} context="เฉพาะรายการในหน้าปัจจุบัน" tone="teal" loading={loading} /><Card label="ประเภทเหตุการณ์" value={categories} context="เฉพาะรายการในหน้าปัจจุบัน" tone="amber" loading={loading} /><Card label="ผู้ดำเนินการ" value={actors} context="เฉพาะรายการในหน้าปัจจุบัน" tone="blue" loading={loading} /></div>; }
