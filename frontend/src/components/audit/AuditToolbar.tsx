type Props = { pageSize: number; onPageSize(value: number): void; search: string; onSearch(value: string): void; onClear(): void; active: boolean };
export function AuditToolbar({ pageSize, onPageSize, search, onSearch, onClear, active }: Props) { return <section className="audit-toolbar" aria-label="เครื่องมือบันทึกการตรวจสอบ"><label className="audit-search"><span aria-hidden="true">⌕</span><input aria-label="ค้นหาในหน้าปัจจุบัน" value={search} onChange={(event) => onSearch(event.target.value)} placeholder="ค้นหาการดำเนินการหรือประเภทข้อมูล" /><small>ค้นหาเฉพาะรายการในหน้าปัจจุบัน</small></label><label className="audit-page-size">แสดง<select aria-label="จำนวนรายการต่อหน้า" value={pageSize} onChange={(event) => onPageSize(Number(event.target.value))}><option value={25}>25</option><option value={50}>50</option><option value={100}>100</option></select> รายการ</label>{active && <button type="button" className="small-action" onClick={onClear}>ล้างคำค้น</button>}</section>; }
