import { useEffect, useRef } from 'react';
import type { AuditEvent } from './AuditTable';
import { formatTime, safe } from './AuditTable';
type Props = { event?: AuditEvent; onClose(): void };
export function AuditEventPreview({ event, onClose }: Props) { const panelRef = useRef<HTMLElement>(null); const closeRef = useRef<HTMLButtonElement>(null); useEffect(() => { if (!event) return; const previous = document.activeElement as HTMLElement | null; closeRef.current?.focus(); const handler = (e: KeyboardEvent) => { if (e.key === 'Escape') { onClose(); return; } if (e.key !== 'Tab' || !panelRef.current) return; const nodes = Array.from(panelRef.current.querySelectorAll<HTMLElement>('button, [href], input, select, textarea, [tabindex]:not([tabindex="-1"])')).filter((node) => !node.hasAttribute('disabled')); if (!nodes.length) return; const first = nodes[0]; const last = nodes[nodes.length - 1]; if (e.shiftKey && document.activeElement === first) { e.preventDefault(); last.focus(); } else if (!e.shiftKey && document.activeElement === last) { e.preventDefault(); first.focus(); } }; window.addEventListener('keydown', handler); return () => { window.removeEventListener('keydown', handler); previous?.focus(); }; }, [event, onClose]); if (!event) return null; return <div className="audit-preview-backdrop" role="presentation" onMouseDown={(e) => { if (e.target === e.currentTarget) onClose(); }}><aside ref={panelRef} className="audit-preview-panel" role="dialog" aria-modal="true" aria-labelledby="audit-preview-title"><header><div><p>EVENT SUMMARY</p><h2 id="audit-preview-title">รายละเอียดเหตุการณ์</h2></div><button ref={closeRef} type="button" className="audit-preview-close" aria-label="ปิดรายละเอียดเหตุการณ์" onClick={onClose}>×</button></header><p className="audit-preview-note">ข้อมูลสรุปจากรายการที่โหลดแล้ว</p><dl><div><dt>วันและเวลา</dt><dd>{formatTime(event.createdAt)}</dd></div><div><dt>ผู้ดำเนินการ</dt><dd>{safe(event.actor?.displayName)}</dd></div><div><dt>บทบาท</dt><dd>{safe(event.actor?.role)}</dd></div><div><dt>การดำเนินการ</dt><dd>{safe(event.action)}</dd></div><div><dt>ประเภทข้อมูล</dt><dd>{safe(event.entityType)}</dd></div></dl><footer><button type="button" className="small-action" onClick={onClose}>ปิด</button></footer></aside></div>; }
